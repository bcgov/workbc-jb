apiVersion: batch/v1
kind: Job
metadata:
  name: jb2-migrate
  namespace: app
spec:
  backoffLimit: 1
  template:
    spec:
      restartPolicy: Never
      containers:
        - name: migrate
          image: __IMAGE__
          envFrom:
            - configMapRef:
                name: jb-config-d2
          env:
            - name: AppSettings__ApplyMigrations
              value: "true"
