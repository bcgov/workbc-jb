; Custom PHP configuration for the Job Alert Notifier CLI.
; The task loads every active alert plus one count-only ES query per alert.
memory_limit = 512M
default_socket_timeout = 60
date.timezone = America/Vancouver
