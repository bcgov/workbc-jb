; Custom PHP configuration for the Federal CLI importer
; The federal feed can return tens of thousands of jobs in --reimport mode.
memory_limit = 1024M
default_socket_timeout = 60
date.timezone = America/Vancouver